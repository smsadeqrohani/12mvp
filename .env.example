# Convex Backend Configuration
# Deployment used by `npx convex dev`
# Format: dev:your-deployment-name or prod:your-deployment-name
CONVEX_DEPLOYMENT=dev:your-deployment-name

# Convex Backend URL - Exposed to client (web, iOS, Android)
# IMPORTANT: Must start with EXPO_PUBLIC_ to be available in Expo apps
# Get this from: https://dashboard.convex.dev or run `npx convex dev`
EXPO_PUBLIC_CONVEX_URL=https://your-deployment.convex.cloud

# Convex Site URL (for OAuth callbacks and deep linking)
# Development: http://localhost:8081 (web) or exp://localhost:8081 (mobile)
# Production: https://yourdomain.com
CONVEX_SITE_URL=http://localhost:8081

# Convex Deploy Key (for production deployments)
# Get this from: https://dashboard.convex.dev
# CONVEX_DEPLOY_KEY=project:your-project:deployment-id|key
